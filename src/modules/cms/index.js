/**
 * Cms Module - Gestion dynamique de contenu (texte, images, sections)
 */

module.exports = {
  routes: require('./routes'),
  controller: require('./controller'),
  service: require('./service'),
};
